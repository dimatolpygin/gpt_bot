# GPT Telegram Bot — Архитектура

Описание архитектуры проекта и актуальных расширений на март 2026.

## 1. Точки входа
- `src/index.js` — запускает Telegraf (бот) и Express (веб-сервер), регистрирует middleware/auth и setupHandlers.
- `src/server.js` — предоставляет `/webapp`, `/gallery`, API `/api/history`, `/api/templates` и `/api/template-select`, валидирует Telegram initData.

## 2. Основной стек
- **Supabase** (`services/supabase.js`) — PostgreSQL с таблицами `bot_*`, `token_*`, `bot_user_settings`, `bot_content`, `buttons`. Содержит обёртки для диалогов, prompts, шаблонов, токенов и истории.
- **Redis** (`services/redis.js`) — хранит быстрые статусы: active conv, model, web search, processing lock, шаблонный путь, генерации изображений/видео.
- **OpenAI + Wavespeed** — `services/openai.js` управляет стримами GPT, web search, vision, voice; `services/wavespeed.js` вызывает NanoBanana, Seedream, Flux2, Seedance, Kling и Hailuo для изображений/видео.
- **Токены** (`services/tokens.js`) — RPC `spend_tokens`/`credit_tokens`, баланс, история, сообщения `notEnoughMsg` для UI.
- **Настройки** (`services/userSettings.js`) — хранит предпочтения по фото/видео в `bot_user_settings`.
- **CMS** (`services/content*.js`) — управляет текстами, Reply/Inline кнопками через таблицу `bot_content`.

## 3. Bot handlers
- `start.js` — `/start`, `main_menu`, `gpt`, `nb_menu`, `vid_menu`, показывает баланс по токенам.
- `chat.js` — основной поток сообщений, голосовых и файлов: получает модель, веб-поиск, списывает токены (chat/transcribe), стримит ответ GPT, отправляет уведомления о списании, добавляет команды `/balance` и `/grant`.
- `callbacks.js` — inline-кнопки (модели, промты, мышление, истории); теперь использует `safeEditOrReply` и обновлённые клавиатуры.
- `dialogs.js` — управление диалогами + WebApp history.
- `nanoBanana.js` и `videoGen.js` — генерации изображений/видео: поддержка GPT Image 1.5, Flux2 Pro, Seedream, токены, шаблоны (WebApp), повтор и редактирование результатов.
- `profile.js` — простой профиль, показывает баланс и кнопку «🏠 В меню».
- `prompts.js` — сохранённые промты с safeEdit, просмотр, создание/удаление через CMS.

## 4. Web-интерфейсы
- `/webapp` — история диалога (src/webapp/index.html).
- `/gallery` — карточки шаблонов (buttons) с lazy-load, фильтрацией по полу и POST `/api/template-select` → сохраняет шаблон в Redis, отправляет превью фото пользователю.
- Все WebApp пути проверяют initData с HMAC-SHA256.

## 5. Клавиатуры
- `bot/keyboards/imageMenuKb.js` — Nano Banana: модели/режимы, GPT15E, Flux2, кнопки CMS, подбор размеров, кнопки repeat/edit.
- `bot/keyboards/videoMenuKb.js` — видео-клавиатуры Seedance/Hailuo/Kling, новые кнопки меню, inline результат.
- `bot/keyboards/profileKb.js` — inline-возврат в меню.
- `bot/keyboards/models.js` — capability map (chat/vision/webSearch). Поддерживает токены/флаги.

## 6. Дополнения
- Токен-система применяется к генерациям: чат (`chat_key`), изображение (`img_*`), видео (`vid_*`) и голос (`transcribe`). Уведомления информируют пользователя о списании.
- Профиль и токеновая панель интегрированы с данными: кол-во токенов, настройки изображений/видео, команда `👤 Профиль` в main keyboard.
- CMS/Redis позволяют динамически менять контент меню, клавиатуры и сохранённые параметры без деплоя.

## 7. Прочее
- `.env` содержит BOT_TOKEN, OPENAI_API_KEY, SUPABASE_URL/KEY, REDIS_URL, WEBAPP_URL, APP_URL, PORT, ALLOWED_USERS, прокси.
- `botInstance.js` хранит глобальный экземпляр Telegraf для Express (используется при template-select).
- `tokens` SQL (`tokens_migration.sql`) и `user_settings.sql` (переименованная таблица) должны быть применены вручную в Supabase.
